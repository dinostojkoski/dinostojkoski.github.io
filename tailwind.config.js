/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: 'class', // Enable class-based dark mode
  content: [
    "./index.html",        // Include the HTML file
    "./assets/**/*.css",   // Include CSS files if needed
  ],
  theme: {
    extend: {
    },
  },
  plugins: [],
};


